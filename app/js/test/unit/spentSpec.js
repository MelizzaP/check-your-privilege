'use strict';

describe('myApp.spent module', function() {

  beforeEach(module('myApp.spent'));

  describe('spent controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('SpentCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
